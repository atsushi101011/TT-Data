<div class="container pt-3">
  <div class="row">
    <div class="col-12">
    <h3><%= t('.title') %></h3>
      <div class="row">
        <% if @pro_games.present? %>
          <%= render @pro_games %>
        <% else %>
          <p>試合がありません。</p>
        <% end %>
      </div>
    </div>
  </div>
</div>